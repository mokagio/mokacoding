:plain
	---
	layout: default
	---

.post-container
	%h2.post-title
		%a{ :href => "{{ site.url}}{{ page.url }}" } {{ page.title }}
	%p#post-date
		{{ page.date | date_to_string }}

	.post
		{{ content }}
