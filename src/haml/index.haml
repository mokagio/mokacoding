:plain
	---
	layout: default
	title: mokacoding
	---

#home
	.posts
		{% for post in site.posts limit:5 %}
		.post-container
			%h2
				%a{ :href => "{{ post.url }}" } {{ post.title }}
			%span
				{{ post.date | date_to_string }}
		{% endfor %}