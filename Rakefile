desc "Watches the Sass and Haml soruces for changes and recompiles"
task :watch do
	source = "src"
	# watch sass
	sass_source = "#{source}/sass"
	sass_destination = "css"
	system "sass --watch #{sass_source}:#{sass_destination}"
	
	# watch haml
end